apiVersion: v1
kind: Service
metadata:
  name: pg-gateway-service
spec:
  type: ClusterIP
  selector:
    app: pg-gateway
  ports:
    - protocol: TCP
      port: 5432
      targetPort: pg-port
      name: pg-traffic
